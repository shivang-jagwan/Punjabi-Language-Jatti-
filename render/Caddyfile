{
	# debug
}

:{$PORT} {
	encode gzip zstd

	@api path /api/*
	handle @api {
		reverse_proxy 127.0.0.1:8000 {
			header_up X-API-Key {$JATTI_API_KEY}
		}
	}

	# Everything else (static UI + /healthz)
	reverse_proxy 127.0.0.1:8000
}
